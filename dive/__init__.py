"""DIVE — Dependency Intelligence for Vulnerability Exposure.

This package bundles every application module (collector, categorizer, scanners,
notifier, web layer, etc.). The HTTP entry point is `dive.main:app`.
"""
