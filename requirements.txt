# Core web framework and server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Scheduler (in-process, no external broker needed)
apscheduler>=3.10.0

# RSS/Atom feed parsing
feedparser>=6.0.11

# Async HTTP client
# Minimum 0.27.0 — earlier versions had SSRF via URL parsing (CVE-2021-41945)
httpx>=0.27.0

# GitHub API
PyGithub>=2.3.0

# YAML config loading
# Minimum 6.0 — earlier versions had RCE via unsafe load (CVE-2020-1747, CVE-2020-14343)
# Always use yaml.safe_load() — never yaml.load()
PyYAML>=6.0

# Multipart form support (required by FastAPI for file uploads and forms)
# Minimum 0.0.20 — earlier versions vulnerable to header injection and ReDoS
# (GHSA-59g5-xgcq-4qw3, GHSA-mj87-hwqh-73pj, GHSA-pp6c-gr5w-3c5g, GHSA-wp53-j4wj-2cfg)
python-multipart>=0.0.20

# File-based lock for concurrent run protection
# Minimum 3.16.0 — earlier versions had known advisories (GHSA-qmgc-5h2g-mvrw, GHSA-w853-jp5j-5j7f)
filelock>=3.16.0

# Jinja2 templating for the web dashboard
# Minimum 3.1.5 — earlier versions vulnerable to sandbox bypass and XSS
# (GHSA-h5c8-rqwp-cp95, GHSA-q2x7-8rv6-6q7h, GHSA-cpwx-vrp4-4pq7, GHSA-gmj6-6f8f-6699, GHSA-h75v-3vvj-5mfj)
jinja2>=3.1.5

# CVSS vector scoring — accurate base-score calculation for OSV severity data
cvss>=3.0
